pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            url 'https://hypertrace.jfrog.io/artifactory/maven'
        }
    }
}

rootProject.name = 'simple-metric-app'
